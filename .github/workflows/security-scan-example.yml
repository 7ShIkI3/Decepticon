name: "Decepticon security scan (example)"

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  security-events: write

jobs:
  decepticon-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: ${{ vars.ENABLE_DECEPTICON_SCAN == 'true' && secrets.ANTHROPIC_API_KEY != '' }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Decepticon scan
        uses: ./.github/actions/decepticon-scan
        with:
          target: "./"
          scan-mode: "quick"
          scope-mode: "diff"
          diff-base: "origin/${{ github.base_ref || 'main' }}"
          fail-on: "high"
          upload-sarif: "true"
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
